apiVersion: apps/v1
kind: Deployment
metadata:
  name: sunbridechoservice
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sunbridechoservice
  template:
    metadata:
      labels:
        app: sunbridechoservice
    spec:
      containers:
      - name: sunbridechoservice
        image: "tesfrt.azurecr.io/anildesai61/sunbird-echo-service:#{Build.BuildId}#"
        imagePullPolicy: Always
        ports:
        - containerPort: 80
